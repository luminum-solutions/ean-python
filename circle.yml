machine:
  timezone:
    Europe/Amsterdam
  python:
    version: 3.4.2

dependencies:
  pre:
    - sudo apt-get update; sudo apt-get install python-setuptools

test:
  override:
    - ./manage.py test -v 2